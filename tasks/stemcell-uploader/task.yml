---

platform: linux

image_resource:
  type: docker-image
  source: {repository: rjain/buildbox}

params:
  DEBUG:
  OM_TARGET:
  OM_USERNAME:
  OM_PASSWORD:
  OM_CLIENT_ID:
  OM_CLIENT_SECRET:
  PIVNET_API_TOKEN:
  IAAS_TYPE:
  STEMCELL_TYPE:

inputs:
  - name: pivnet-product
  - name: pipelines-repo
  - name: om-cli
  - name: pivnet-cli
  - name: jq

run:
  path: pipelines-repo/tasks/stemcell-uploader/task.sh
