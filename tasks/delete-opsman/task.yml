---

platform: linux

image_resource:
  type: docker-image
  source: {repository: rjain/buildbox}

params:
  OPS_MAN_IP:
  VCENTER_HOST:
  VCENTER_USR:
  VCENTER_PWD:

inputs:
  - name: concourse-vsphere

run:
  path: concourse-vsphere/delete-opsman/task.sh
