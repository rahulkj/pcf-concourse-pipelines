---

platform: linux

image_resource:
  type: docker-image
  source: {repository: rjain/buildbox}

params:
  DEBUG:
  OM_TARGET:
  OM_USERNAME:
  OM_PASSWORD:
  OM_CLIENT_ID:
  OM_CLIENT_SECRET:

inputs:
  - name: product
  - name: pipelines-repo
  - name: om-cli

run:
  path: pipelines-repo/tasks/upload-product/task.sh
