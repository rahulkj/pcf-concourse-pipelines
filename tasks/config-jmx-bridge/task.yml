---

platform: linux

image_resource:
  type: docker-image
  source: {repository: rjain/buildbox}

params:
  PRODUCT_IDENTIFIER:
  OPS_MGR_HOST:
  OPS_MGR_USR:
  OPS_MGR_PWD:
  AZ_NAME:
  NETWORK_NAME:
  JMX_ADMIN_USR:
  JMX_ADMIN_PWD:
  NAT_ENABLED:
  NAT_JMX_BRIDGE_IP:
  SECURITY_LOGGING_ENABLED:
  JMX_USE_SSL:
  SSL_CERT:
  SSL_PRIVATE_KEY:
  JMX_DOMAIN:
  USE_METRIC_PREFIX:
  MAXIMUS_INSTANCE_TYPE:
  MAXIMUS_INSTANCES:
  JMX_FIREHOSE_NOZZLE_INSTANCE_TYPE:
  JMX_FIREHOSE_NOZZLE_INSTANCES:
  INTEGRATION_TESTS_INSTANCE_TYPE:
  INTEGRATION_TESTS_INSTANCES:

inputs:
  - name: concourse-vsphere
  - name: om-cli

run:
  path: concourse-vsphere/tasks/config-jmx-bridge/task.sh
